---
title: 'Events'
---
<div class="events">
  <h1>Events</h1>
  <p>These are some of the events I have spoken at before. You can find more information on <%= link_to "Lanyrd", data.social.lanyrd %>.</p>
  <p>Please feel free to <%= mail_to data.social.mail, 'send me an email', :subject => 'Hello' %> in case you'd like me to present at your event.</p>
  <% data.events.years.each do |year| %>
    <h3 class="page-header"><%= year.year %></h3>
    <% year.events.each do |event| %>
      <div class="row event">
        <div class="col-md-3"><%= event.date %></div>
        <div class="col-md-6"><%= event.name %></div>
        <div class="col-md-3"><%= event.location %></div>
      </div>
    <% end %>
  <% end %>
</div>
